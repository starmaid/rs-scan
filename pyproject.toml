[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rs-scan"
version = "0.1.0"
description = "Simple RealSense scan helper"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
  "pyrealsense2",
  "opencv-python"
]

